#!/usr/bin/env bash
APP_DIR=$( cd "$( dirname "${BASH_SOURCE[0]}" )" && cd .. && pwd )

if [ -z "$DATA_DIR" ]; then
  echo "DATA_DIR environment variable must be set"
  exit 1
fi

export LD_LIBRARY_PATH=${APP_DIR}/php/lib:${APP_DIR}/postgresql/lib
export NEXTCLOUD_CONFIG_DIR=${DATA_DIR}/nextcloud/comfig
${APP_DIR}/php/bin/php -c ${DATA_DIR}/config/php.ini $@